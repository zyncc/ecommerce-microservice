apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: auth
  ports:
    - port: {{ .Values.auth.port }}
      targetPort: {{ .Values.auth.port }}
  type: ClusterIP
