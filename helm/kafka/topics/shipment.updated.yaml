apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: shipment.updated
  namespace: confluent
spec:
  kafkaClusterRef:
    name: kafka
  replicas: 1
  partitionCount: 2
  configs:
    cleanup.policy: delete