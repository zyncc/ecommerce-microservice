apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: payment.succeeded
  namespace: confluent
spec:
  kafkaClusterRef:
    name: kafka
  replicas: 1
  partitionCount: 4
  configs:
    cleanup.policy: delete