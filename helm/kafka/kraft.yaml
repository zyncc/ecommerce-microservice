apiVersion: platform.confluent.io/v1beta1
kind: KRaftController
metadata:
  name: kraft
  namespace: kafka
spec:
  replicas: 1

  dataVolumeCapacity: 10Gi

  image:
    application: confluentinc/cp-server:8.1.0
    init: confluentinc/confluent-init-container:2.13.0
